/* 
  Реализовать функцию, которая принимает два строковых параметра и возвращает строку с большей длинной, если они равны, то возвращается первая строка.
*/

export function longerString(a, b) {
  return a
}

